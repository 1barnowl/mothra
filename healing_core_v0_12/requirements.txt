# HealingCore v0.8 — Python ≥ 3.9 required
# All items below are OPTIONAL unless marked (required)
# The core runs on stdlib alone; extras unlock features.

# ── ML Classifier (v0.8) ─────────────────────────────────────────────────────
scikit-learn>=1.3     # TF-IDF + LogisticRegression classifier
                      # Without this: regex-only classification (fully functional)

# ── AI / Knowledge ───────────────────────────────────────────────────────────
anthropic>=0.20       # Claude API for knowledge core & multi-AI oracle
                      # Without this: local knowledge + DuckDuckGo fallback only

# ── Observability ─────────────────────────────────────────────────────────────
psutil>=5.9           # CPU / memory / disk metrics for HealthMonitor + Canary
                      # Without this: poll_metrics() returns empty hardware stats

opentelemetry-sdk>=1.20           # Distributed tracing (OTLP export)
opentelemetry-exporter-otlp>=1.20 # OTLP gRPC/HTTP exporter
                                  # Without this: tracing buffers in memory only

# ── Structured logging ────────────────────────────────────────────────────────
python-json-logger>=2.0  # JSON-structured log lines (optional)

# ── YAML policy loading ────────────────────────────────────────────────────────
PyYAML>=6.0           # Required for healing_policy.yaml (required)

# ── HTTP server (API + Prometheus) ────────────────────────────────────────────
# Stdlib http.server is used by default — no extra dep needed.

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=7.0
